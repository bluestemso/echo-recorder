---
phase: 06-popover-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EchoRecorder/UI/MenuBar/RecordingViewModel.swift
  - EchoRecorder/UI/Finalize/FinalizeView.swift
  - EchoRecorder/UI/Finalize/FinalizeRecordingViewModel.swift
  - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
  - EchoRecorderTests/UI/FinalizeViewStateTests.swift
  - EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift
  - EchoRecorderTests/UI/RecordingViewModelTests.swift
autonomous: true
requirements: [POPOV-01, POPOV-02, POPOV-03]
must_haves:
  truths:
    - "After stop completes, popover shows a finalize-focused panel instead of live meters/gain controls"
    - "Primary finalize action is a full-width button labeled 'Save Recording'"
    - "During save, finalize panel shows inline spinner + text 'Saving recording...' and blocks duplicate saves"
    - "After a successful save, user sees explicit success feedback before UI resets"
    - "Edited recording name in finalize is the filename used by finalizeRecording"
  artifacts:
    - path: "EchoRecorder/UI/MenuBar/RecordingViewModel.swift"
      provides: "Finalize UI state machine (editing/saving/success), editable pending finalize name, and post-save reset timing"
      contains: "confirmFinalize"
    - path: "EchoRecorder/UI/Finalize/FinalizeView.swift"
      provides: "Finalize-first UI with editable name field, two-line path block, inline change-location action, and stacked full-width primary action"
      contains: "Save Recording"
    - path: "EchoRecorderTests/UI/FinalizeViewStateTests.swift"
      provides: "Automated UI-contract assertions for finalize action hierarchy and saving/success states"
      contains: "testFinalizeView"
  key_links:
    - from: "EchoRecorder/UI/Finalize/FinalizeView.swift"
      to: "EchoRecorder/UI/MenuBar/RecordingViewModel.swift"
      via: "binding/callbacks for editable recording name and finalize state"
      pattern: "pendingFinalizeName|finalizeUIState"
    - from: "EchoRecorder/UI/MenuBar/RecordingViewModel.swift"
      to: "RecorderCoordinator.finalizeRecording"
      via: "confirmFinalize uses edited name + selectedDirectory"
      pattern: "finalizeRecording\(recordingName:"
---

<objective>
Implement the finalize UX state contract so post-recording flow is focused, clear, and deterministic: editable recording name, stronger action hierarchy, visible save progress, and success feedback with delayed reset.

Purpose: Satisfy POPOV finalize UX requirements without adding new product capabilities or changing recorder core architecture.
Output: Updated finalize-first UI and view-model state transitions with automated guardrails.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-popover-ux-improvements/06-CONTEXT.md
@.planning/phases/06-popover-ux-improvements/06-RESEARCH.md
@.planning/phases/06-popover-ux-improvements/06-VALIDATION.md
@.planning/phases/03-save-location-picker/03-SUMMARY.md
@EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
@EchoRecorder/UI/Finalize/FinalizeView.swift
@EchoRecorder/UI/Finalize/FinalizeRecordingViewModel.swift
@EchoRecorder/UI/MenuBar/RecordingViewModel.swift
@EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift

<interfaces>
From EchoRecorder/UI/MenuBar/RecordingViewModel.swift:
```swift
@Published private(set) var pendingFinalize: FinalizeRecordingViewModel?
private(set) var activeRecordingName: String?
func confirmFinalize()
```

From EchoRecorder/UI/Finalize/FinalizeView.swift:
```swift
struct FinalizeView: View {
    @ObservedObject var viewModel: FinalizeRecordingViewModel
    let recordingName: String
    let onSave: () -> Void
}
```

From EchoRecorder/UI/Finalize/FinalizeRecordingViewModel.swift:
```swift
@Published var selectedDirectory: URL
var displayPath: String { get }
func chooseDirectory()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 finalize UX test scaffolds before implementation</name>
  <read_first>
    - EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift
    - EchoRecorderTests/UI/RecordingViewModelTests.swift
    - EchoRecorder/UI/Finalize/FinalizeView.swift
    - EchoRecorder/UI/MenuBar/RecordingViewModel.swift
    - .planning/phases/06-popover-ux-improvements/06-CONTEXT.md
  </read_first>
  <files>EchoRecorderTests/UI/FinalizeViewStateTests.swift, EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift, EchoRecorderTests/UI/RecordingViewModelTests.swift</files>
  <behavior>
    - Test 1: Finalize primary action contract uses `Save Recording` label and full-width stacked action hierarchy.
    - Test 2: Saving state renders inline progress copy exactly `Saving recording...` and disables primary save action.
    - Test 3: Success state is visible before reset and reset delay is approximately 1.5 seconds.
    - Test 4: Edited finalize name is passed to `RecorderCoordinator.finalizeRecording(recordingName:)`.
  </behavior>
  <action>Create `EchoRecorderTests/UI/FinalizeViewStateTests.swift` with failing tests for finalize view states: idle/editing, saving, and success. Add/extend runtime and view-model tests to assert concrete contract values: button title `Save Recording`, progress text `Saving recording...`, and post-success reset delay target `1.5` seconds (tolerance `+/-0.2s`). Add a fake finalizer/coordinator capture to assert the edited name value is the exact `recordingName` argument used in finalize. Keep each test under 2s runtime.</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeViewStateTests -only-testing:EchoRecorderTests/UI/RecordingRuntimeFlowTests -only-testing:EchoRecorderTests/UI/RecordingViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorderTests/UI/FinalizeViewStateTests.swift` exists and declares `final class FinalizeViewStateTests: XCTestCase`
    - Test file contains method names matching `testFinalizeView` and at least one method containing `Saving recording...`
    - `EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift` contains an assertion that verifies a success-reset delay around `1.5`
    - `EchoRecorderTests/UI/RecordingViewModelTests.swift` contains an assertion verifying edited finalize name is used for finalize call
    - `<automated>` command exits 0 after implementation in Task 3
  </acceptance_criteria>
  <done>Wave 0 coverage exists for finalize UX contracts and would fail without the new finalize state behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement finalize-first panel with editable name, save progress, and success morph</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/RecordingViewModel.swift
    - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
    - EchoRecorder/UI/Finalize/FinalizeView.swift
    - EchoRecorder/UI/Finalize/FinalizeRecordingViewModel.swift
    - .planning/phases/06-popover-ux-improvements/06-CONTEXT.md
  </read_first>
  <files>EchoRecorder/UI/MenuBar/RecordingViewModel.swift, EchoRecorder/UI/MenuBar/RecordingPopoverView.swift, EchoRecorder/UI/Finalize/FinalizeView.swift, EchoRecorder/UI/Finalize/FinalizeRecordingViewModel.swift</files>
  <behavior>
    - Test 1: Stop completion enters finalize-first mode and hides live meter/gain controls.
    - Test 2: Primary action is full-width `Save Recording`, becomes disabled loading state with spinner + `Saving recording...` while finalize runs.
    - Test 3: On finalize success, primary action morphs to success state, then UI resets to idle/start after ~1.5s.
    - Test 4: Edited name field value is the source-of-truth name sent to finalize.
  </behavior>
  <action>In `RecordingViewModel`, add explicit finalize UI state enum with exactly three cases: `editing`, `saving`, `success`; add `@Published var pendingFinalizeName: String` and initialize it from `recordingName` when stop completes. Update `confirmFinalize()` to: (1) trim `pendingFinalizeName`, fallback to original `activeRecordingName` only if trimmed value is empty, (2) set state to `saving`, (3) call `coordinator.finalizeRecording(recordingName: resolvedName, overrideDirectory: finalizeVM.selectedDirectory)`, (4) on success set state to `success`, persist `lastFinalizedOutput`, wait `1.5` seconds, then clear `pendingFinalize`, `activeRecordingName`, and `pendingFinalizeName`, (5) on failure set `latestErrorDescription` and return state to `editing` without dropping finalize UI. In `RecordingPopoverView`, switch to finalize-first composition: when `pendingFinalize` exists render only finalize content block; otherwise render existing error/device/meter/gain/start-stop content. In `FinalizeView`, replace current row with a finalize panel containing: editable `TextField("Recording name", text: ...)`, two-line save path block (line 1 label + line 2 truncated path), inline icon action `Change Location`, stacked full-width primary button labeled `Save Recording` in editing state, inline `ProgressView` + `Saving recording...` in saving state, and success morph state with checkmark icon after successful save.</action>
  <verify>
    <automated>xcodebuild build -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorder/UI/MenuBar/RecordingViewModel.swift` contains a finalize UI state enum with cases `editing`, `saving`, and `success`
    - `EchoRecorder/UI/MenuBar/RecordingViewModel.swift` contains `pendingFinalizeName` and uses it in `finalizeRecording(recordingName:` call
    - `EchoRecorder/UI/MenuBar/RecordingViewModel.swift` contains a concrete `1.5` second delay before clearing finalize state
    - `EchoRecorder/UI/Finalize/FinalizeView.swift` contains the exact strings `Save Recording`, `Saving recording...`, and `Change Location`
    - `EchoRecorder/UI/Finalize/FinalizeView.swift` contains an editable `TextField` for recording name
    - `EchoRecorder/UI/MenuBar/RecordingPopoverView.swift` contains finalize-first branching where live meters/gain controls are not rendered when `pendingFinalize` exists
    - `<automated>` command exits 0
  </acceptance_criteria>
  <done>Finalize UX behavior is deterministic and satisfies POPOV-01/02/03 with explicit state transitions and user-visible feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Green the finalize UX test suite and lock regressions</name>
  <read_first>
    - EchoRecorderTests/UI/FinalizeViewStateTests.swift
    - EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift
    - EchoRecorderTests/UI/RecordingViewModelTests.swift
    - EchoRecorder/UI/MenuBar/RecordingViewModel.swift
    - EchoRecorder/UI/Finalize/FinalizeView.swift
  </read_first>
  <files>EchoRecorderTests/UI/FinalizeViewStateTests.swift, EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift, EchoRecorderTests/UI/RecordingViewModelTests.swift</files>
  <action>Complete all failing assertions introduced in Task 1 so tests verify concrete behavior: finalize-first action hierarchy, saving inline progress text, success morph visibility, 1.5-second reset, and edited-name finalize call. Keep new tests deterministic by controlling delays through injected time seams or bounded `Task.sleep` windows with explicit tolerances. Ensure no test depends on wall-clock sleeps longer than 2 seconds.</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeViewStateTests -only-testing:EchoRecorderTests/UI/RecordingRuntimeFlowTests -only-testing:EchoRecorderTests/UI/RecordingViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `<automated>` command exits 0
    - `EchoRecorderTests/UI/FinalizeViewStateTests.swift` contains assertions for all three states: editing, saving, success
    - `EchoRecorderTests/UI/RecordingRuntimeFlowTests.swift` contains a passing test that observes success state before reset
    - `EchoRecorderTests/UI/RecordingViewModelTests.swift` contains a passing assertion for edited-name finalize call path
  </acceptance_criteria>
  <done>Automated regression coverage for POPOV-01/02/03 is in place and green.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeViewStateTests -only-testing:EchoRecorderTests/UI/RecordingRuntimeFlowTests -only-testing:EchoRecorderTests/UI/RecordingViewModelTests` and confirm finalize-focused flow contracts remain green.
</verification>

<success_criteria>
- POPOV-01: Finalize actions are stacked and clear, with full-width primary `Save Recording` action.
- POPOV-02: Saving state shows inline spinner + exact copy `Saving recording...` and prevents duplicate save.
- POPOV-03: Success feedback appears and remains visible before a delayed reset to idle/start.
</success_criteria>

<output>
After completion, create `.planning/phases/06-popover-ux-improvements/06-01-SUMMARY.md`
</output>
