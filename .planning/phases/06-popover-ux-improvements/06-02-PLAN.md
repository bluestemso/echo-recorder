---
phase: 06-popover-ux-improvements
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - EchoRecorder/UI/MenuBar/PopoverUXTiming.swift
  - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
  - EchoRecorder/UI/MenuBar/StatusItemController.swift
  - EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift
  - EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift
  - EchoRecorderTests/UI/StatusItemControllerIconTests.swift
autonomous: false
requirements: [POPOV-04, POPOV-05, ANIM-01, ANIM-02, ANIM-03]
must_haves:
  truths:
    - "Transition from recording content to finalize panel starts only after stop completes"
    - "Popover open/close feels near-instant and no long fade is perceivable"
    - "Content transition duration is fixed at 150ms and stays under 200ms"
    - "Reduce Motion minimizes/removes movement while preserving visible state changes"
  artifacts:
    - path: "EchoRecorder/UI/MenuBar/PopoverUXTiming.swift"
      provides: "Single source of truth for popover fade (0.075), content transition (0.15), and success reset (1.5) durations"
      contains: "contentTransitionDuration"
    - path: "EchoRecorder/UI/MenuBar/RecordingPopoverView.swift"
      provides: "Fade+move transition policy with reduce-motion fallback"
      contains: "accessibilityReduceMotion"
    - path: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      provides: "Popover show/close animation policy configuration"
      contains: "togglePopover"
    - path: "EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift"
      provides: "Automated checks for popover animation policy constants and controller behavior"
      contains: "testPopover"
  key_links:
    - from: "EchoRecorder/UI/MenuBar/RecordingViewModel.swift"
      to: "EchoRecorder/UI/MenuBar/RecordingPopoverView.swift"
      via: "pendingFinalize drives content switch post-stop"
      pattern: "pendingFinalize"
    - from: "EchoRecorder/UI/MenuBar/PopoverUXTiming.swift"
      to: "EchoRecorder/UI/MenuBar/RecordingPopoverView.swift"
      via: "transition animation duration constant"
      pattern: "0\.15|contentTransitionDuration"
    - from: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      to: "NSPopover"
      via: "open/close animation policy"
      pattern: "animates|NSAnimationContext"
---

<objective>
Implement and verify the popover transition-speed contract: near-instant show/close behavior, 150ms content transitions, and reduce-motion-safe animation policy for recording-to-finalize flow.

Purpose: Close POPOV transition and ANIM timing requirements with deterministic constants and explicit policy tests.
Output: Shared timing contract + popover/controller transition wiring + automated and manual validation.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-popover-ux-improvements/06-CONTEXT.md
@.planning/phases/06-popover-ux-improvements/06-RESEARCH.md
@.planning/phases/06-popover-ux-improvements/06-VALIDATION.md
@.planning/phases/06-popover-ux-improvements/06-01-SUMMARY.md
@EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
@EchoRecorder/UI/MenuBar/StatusItemController.swift
@EchoRecorderTests/UI/StatusItemControllerIconTests.swift

<interfaces>
From EchoRecorder/UI/MenuBar/StatusItemController.swift:
```swift
private let popover: NSPopover
@objc private func togglePopover(_ sender: AnyObject?)
```

From EchoRecorder/UI/MenuBar/RecordingPopoverView.swift:
```swift
struct RecordingPopoverView: View {
    @ObservedObject var viewModel: RecordingViewModel
}
```

From locked user decision (06-CONTEXT.md):
```text
Popover open/close uses a near-instant fade (~75ms).
Recording-to-finalize content transition uses fade + slight movement at 150ms.
Respect macOS Reduce Motion preference by minimizing/removing movement.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 timing-policy test scaffolds</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
    - EchoRecorderTests/UI/StatusItemControllerIconTests.swift
    - .planning/phases/06-popover-ux-improvements/06-CONTEXT.md
    - .planning/phases/06-popover-ux-improvements/06-VALIDATION.md
  </read_first>
  <files>EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift, EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift</files>
  <behavior>
    - Test 1: Content transition duration constant equals `0.15` seconds and never exceeds `0.2`.
    - Test 2: Popover open/close fade duration constant equals `0.075` seconds.
    - Test 3: Reduce Motion policy removes movement while retaining opacity/state transition.
    - Test 4: Popover controller applies explicit animation policy at show and close paths.
  </behavior>
  <action>Create failing test scaffolds: `EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift` for content transition and reduce-motion policy, and `EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift` for popover fade policy. Include concrete constant assertions `XCTAssertEqual(..., 0.15, accuracy: 0.001)` and `XCTAssertEqual(..., 0.075, accuracy: 0.001)`. Include a test case that asserts reduce-motion branch disables movement while keeping opacity transition active. Keep tests deterministic (no visual timing sleeps > 0.2s).</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeTransitionTimingTests -only-testing:EchoRecorderTests/UI/PopoverAnimationPolicyTests</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift` exists and declares `final class FinalizeTransitionTimingTests: XCTestCase`
    - `EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift` exists and declares `final class PopoverAnimationPolicyTests: XCTestCase`
    - New test files contain explicit numeric assertions for `0.15` and `0.075`
    - Test file contains a method name including `ReduceMotion`
    - `<automated>` command exits 0 after Task 2 implementation
  </acceptance_criteria>
  <done>Wave 0 timing-policy tests exist and define exact duration targets before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement centralized timing constants and popover/content transition policy</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - EchoRecorder/UI/MenuBar/RecordingViewModel.swift
    - EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift
    - EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift
    - .planning/phases/06-popover-ux-improvements/06-CONTEXT.md
  </read_first>
  <files>EchoRecorder/UI/MenuBar/PopoverUXTiming.swift, EchoRecorder/UI/MenuBar/RecordingPopoverView.swift, EchoRecorder/UI/MenuBar/StatusItemController.swift, EchoRecorderTests/UI/FinalizeTransitionTimingTests.swift, EchoRecorderTests/UI/PopoverAnimationPolicyTests.swift, EchoRecorderTests/UI/StatusItemControllerIconTests.swift</files>
  <behavior>
    - Test 1: Recording-to-finalize transition uses `Animation.easeInOut(duration: 0.15)` and only begins when `pendingFinalize` appears after stop.
    - Test 2: Reduce Motion uses opacity-only transition (no move edge transform).
    - Test 3: Popover open and close use near-instant fade policy at `0.075` seconds.
  </behavior>
  <action>Add `EchoRecorder/UI/MenuBar/PopoverUXTiming.swift` with exact constants: `popoverFadeDuration = 0.075`, `contentTransitionDuration = 0.15`, and `postSaveResetDelay = 1.5`. In `RecordingPopoverView`, add `@Environment(\.accessibilityReduceMotion)` and define transition policy: normal mode uses `AnyTransition.opacity.combined(with: .move(edge: .bottom))`; reduce-motion mode uses `AnyTransition.opacity` only. Apply `withAnimation(.easeInOut(duration: PopoverUXTiming.contentTransitionDuration))` (or equivalent `.animation(..., value:)`) on finalize/non-finalize content switch keyed to `viewModel.pendingFinalize != nil`, so transition starts only after `pendingFinalize` is set post-stop. In `StatusItemController`, honor locked user decision by implementing near-instant fade around popover show/close at `0.075` seconds (research suggested `popover.animates = false`; keep that disabled to avoid default long AppKit animation, then apply explicit alpha fade on hosted content). Add/update tests to assert constants and policy branches. Do not introduce durations other than `0.075`, `0.15`, and `1.5` for this flow.</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeTransitionTimingTests -only-testing:EchoRecorderTests/UI/PopoverAnimationPolicyTests -only-testing:EchoRecorderTests/UI/StatusItemControllerIconTests</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorder/UI/MenuBar/PopoverUXTiming.swift` exists and contains `0.075`, `0.15`, and `1.5` constants
    - `EchoRecorder/UI/MenuBar/RecordingPopoverView.swift` contains `accessibilityReduceMotion` and an opacity-only reduce-motion transition branch
    - `EchoRecorder/UI/MenuBar/RecordingPopoverView.swift` contains `easeInOut(duration: 0.15)` (or equivalent using `PopoverUXTiming.contentTransitionDuration`)
    - `EchoRecorder/UI/MenuBar/StatusItemController.swift` contains popover animation policy code with `popover.animates = false`
    - `EchoRecorder/UI/MenuBar/StatusItemController.swift` contains explicit show/close fade duration path using `0.075`
    - `<automated>` command exits 0
  </acceptance_criteria>
  <done>POPOV-04/05 and ANIM-01/02/03 are enforced through centralized timing constants and tested transition policies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify near-instant popover behavior and 150ms finalize transition on real runtime</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - EchoRecorder/UI/MenuBar/RecordingPopoverView.swift
    - EchoRecorder/UI/MenuBar/PopoverUXTiming.swift
    - .planning/phases/06-popover-ux-improvements/06-CONTEXT.md
  </read_first>
  <files>EchoRecorder/UI/MenuBar/StatusItemController.swift, EchoRecorder/UI/MenuBar/RecordingPopoverView.swift, EchoRecorder/UI/MenuBar/PopoverUXTiming.swift</files>
  <action>After Task 2 tests pass, run app-level visual validation for animation policy. Validate open/close behavior is perceptually instant with only a near-instant fade and validate recording->finalize transition uses fast fade + slight move in normal motion, opacity-only in Reduce Motion mode.</action>
  <what-built>Centralized timing constants, popover show/close fade policy, and reduce-motion-aware finalize content transition policy.</what-built>
  <how-to-verify>
    1) Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeTransitionTimingTests -only-testing:EchoRecorderTests/UI/PopoverAnimationPolicyTests -only-testing:EchoRecorderTests/UI/StatusItemControllerIconTests` and confirm pass.
    2) Launch app and toggle popover 10 times from idle and pending finalize; verify open/close has no long/default fade and feels near-instant.
    3) Start recording, then stop; verify finalize panel appears only after stop completes and transition feels fast (about 150ms) with slight movement.
    4) Enable macOS Reduce Motion and repeat step 3; verify movement is removed/minimized while state change remains visible via opacity.
  </how-to-verify>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeTransitionTimingTests -only-testing:EchoRecorderTests/UI/PopoverAnimationPolicyTests -only-testing:EchoRecorderTests/UI/StatusItemControllerIconTests</automated>
  </verify>
  <acceptance_criteria>
    - Automated verification command exits 0 before manual sign-off
    - Human confirms popover open/close has no perceptible long fade in 10 toggles
    - Human confirms finalize transition starts after stop completion and appears fast (target 150ms)
    - Human confirms Reduce Motion path removes/minimizes movement while preserving state visibility
  </acceptance_criteria>
  <resume-signal>Type "approved" if checks pass, or report failed step number with observed behavior.</resume-signal>
  <done>Manual UX verification confirms runtime behavior for ANIM and transition requirements on target macOS environment.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/UI/FinalizeTransitionTimingTests -only-testing:EchoRecorderTests/UI/PopoverAnimationPolicyTests -only-testing:EchoRecorderTests/UI/StatusItemControllerIconTests`, then complete the runtime animation checkpoint.
</verification>

<success_criteria>
- POPOV-04: recording-to-finalize content switch is smooth and starts after stop completes.
- POPOV-05 / ANIM-03: content transitions are fixed at 150ms and remain under 200ms.
- ANIM-01 / ANIM-02: popover show/close uses near-instant behavior with no long default fade.
</success_criteria>

<output>
After completion, create `.planning/phases/06-popover-ux-improvements/06-02-SUMMARY.md`
</output>
