---
phase: 05-menu-bar-iconography-recording-indicator
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - EchoRecorder/UI/MenuBar/StatusItemController.swift
  - EchoRecorderTests/UI/StatusItemControllerIconTests.swift
autonomous: false
requirements: [ICON-04, IND-02, ICON-03, IND-01, IND-03]
must_haves:
  truths:
    - "Menu bar icon updates within 100ms when RecorderCoordinator.state changes"
    - "Recording state shows a filled red pill behind the icon with no stroke"
    - "Red pill is not visible in idle, preparing, pendingFinalize, or finalizing"
    - "Accessibility label announces current recorder state"
  artifacts:
    - path: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      provides: "State subscription, icon rendering, red-pill rendering, and accessibility updates"
      contains: "recorderCoordinator.$state"
    - path: "EchoRecorderTests/UI/StatusItemControllerIconTests.swift"
      provides: "Timing and appearance-token verification seams"
      contains: "testIconUpdateLatencyWithin100ms"
  key_links:
    - from: "EchoRecorder/Core/Recording/RecorderCoordinator.swift"
      to: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      via: "Combine subscription on recorderCoordinator.$state"
      pattern: "\$state"
    - from: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      to: "EchoRecorder/UI/MenuBar/StatusItemVisualState.swift"
      via: "statusItemVisualState(for:appName:)"
      pattern: "statusItemVisualState\(for:"
    - from: "EchoRecorder/UI/MenuBar/StatusItemController.swift"
      to: "NSStatusBarButton accessibility"
      via: "setAccessibilityLabel"
      pattern: "setAccessibilityLabel"
---

<objective>
Wire the visual-state contract into the real status item so iconography, recording pill, contrast behavior, and latency requirements hold in the running app.

Purpose: Deliver visible in-product behavior for ICON-04 and IND-02 without changing recorder core logic.
Output: Updated `StatusItemController` rendering path + integration-style tests + manual contrast verification checkpoint.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-menu-bar-iconography-recording-indicator/05-CONTEXT.md
@.planning/phases/05-menu-bar-iconography-recording-indicator/05-RESEARCH.md
@.planning/phases/05-menu-bar-iconography-recording-indicator/05-01-SUMMARY.md
@EchoRecorder/UI/MenuBar/StatusItemController.swift
@EchoRecorder/UI/MenuBar/StatusItemVisualState.swift
@EchoRecorder/Core/Recording/RecorderCoordinator.swift

<interfaces>
From EchoRecorder/UI/MenuBar/StatusItemController.swift:
```swift
protocol StatusItemControlling: AnyObject {
    var configuredTitle: String { get }
}

@MainActor
final class StatusItemController: NSObject, StatusItemControlling {
    private(set) var statusItem: NSStatusItem
}
```

From EchoRecorder/UI/MenuBar/StatusItemVisualState.swift (from plan 05-01):
```swift
struct StatusItemVisualState {
    let symbolName: String
    let symbolWeight: NSFont.Weight
    let showRecordingPill: Bool
    let usesPaletteColor: Bool
    let accessibilityLabel: String
    let isAnimated: Bool
}

func statusItemVisualState(for state: RecorderState, appName: String) -> StatusItemVisualState
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace title-based status rendering with state-driven symbol + red pill</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - EchoRecorder/UI/MenuBar/StatusItemVisualState.swift
    - EchoRecorder/Core/Recording/RecorderCoordinator.swift
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-CONTEXT.md
  </read_first>
  <files>EchoRecorder/UI/MenuBar/StatusItemController.swift</files>
  <action>Remove `statusItem.button?.title` state updates and subscribe to `recorderCoordinator.$state` on `RunLoop.main`; for each state, compute `statusItemVisualState(for: state, appName: configuredTitle)` and apply icon immediately using `NSImage(systemSymbolName:)` configured at pointSize `14` and weight `.medium`; wire animation playback directly in `StatusItemController` based on `visualState.isAnimated` so `.preparing`, `.recording`, and `.finalizing` animate while `.idle` and `.pendingFinalize` do not; implement continuous playback in `.recording` (repeat forever until state leaves recording) and finite transition animation for `.preparing`/`.finalizing`; for `.recording`, apply colored `record.circle.fill` and show a filled pill (no stroke) with quick alpha transition duration `0.12`; for non-recording states set pill alpha to `0`; keep red pill hidden for `.idle`, `.preparing`, `.pendingFinalize`, `.finalizing`; update button accessibility label on every state change to mapper text; keep popover toggle behavior unchanged.</action>
  <verify>
    <automated>xcodebuild build -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorder/UI/MenuBar/StatusItemController.swift` no longer contains `"Echo *"`
    - File contains `recorderCoordinator.$state` and `.receive(on: RunLoop.main)`
    - File contains `statusItemVisualState(for:` and `setAccessibilityLabel(`
    - File contains a concrete fade duration value between `0.10` and `0.15` seconds (planned: `0.12`)
    - File contains red-pill visibility branch that is true only for `.recording`
    - File contains explicit animation start/stop wiring keyed by `visualState.isAnimated`
    - File contains continuous animation configuration for `.recording` (repeat-until-state-change)
  </acceptance_criteria>
  <done>Status item visual output is fully state-driven and matches locked icon + indicator timing decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add controller tests for latency and appearance-aware recording indicator</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - EchoRecorderTests/UI/RecordingViewModelTests.swift
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-RESEARCH.md
    - EchoRecorderTests/UI/StatusItemControllerIconTests.swift
  </read_first>
  <files>EchoRecorderTests/UI/StatusItemControllerIconTests.swift</files>
  <action>Create `StatusItemControllerIconTests` with deterministic seams (test doubles/spies) to verify: `testIconUpdateLatencyWithin100ms` (publish state change and assert icon application timestamp delta <= 0.1s), `testRecordingIndicatorUsesAppearanceAwareColorToken` (assert light and dark appearance branches return different red token values that maintain medium emphasis), `testRecordingStateSetsAccessibilityLabel`, `testPreparingAndFinalizingTriggerAnimationPlayback`, and `testRecordingAnimationIsContinuousUntilStateExit`; avoid sleeping longer than 200ms per assertion and keep suite runtime under 10s.</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemControllerIconTests</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorderTests/UI/StatusItemControllerIconTests.swift` exists and declares `final class StatusItemControllerIconTests: XCTestCase`
    - File contains test methods `testIconUpdateLatencyWithin100ms`, `testRecordingIndicatorUsesAppearanceAwareColorToken`, `testPreparingAndFinalizingTriggerAnimationPlayback`, and `testRecordingAnimationIsContinuousUntilStateExit`
    - `<automated>` command exits 0
    - Latency assertion checks `<= 0.1` seconds in test code
  </acceptance_criteria>
  <done>ICON-04 and IND-02 have automated guardrails and regressions fail fast in CI/local test runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify recording indicator visibility matrix on real desktop backgrounds</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-CONTEXT.md
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-RESEARCH.md
  </read_first>
  <files>EchoRecorder/UI/MenuBar/StatusItemController.swift, EchoRecorderTests/UI/StatusItemControllerIconTests.swift</files>
  <action>After Tasks 1-2 pass, run the app and perform a visual QA matrix in light and dark menu bar appearances with at least three wallpaper types (bright, dark, high-detail); verify red pill is clearly visible only while recording and disappears immediately after stop; verify preparing/finalizing animate and recording animation remains continuous until stop.</action>
  <what-built>State-driven symbol iconography, recording-only red pill, state-driven icon animation playback, and accessibility label updates in status item controller with automated latency checks.</what-built>
  <how-to-verify>
    1) Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemVisualStateTests -only-testing:EchoRecorderTests/StatusItemControllerIconTests` and confirm pass.
    2) Launch app, trigger preparing and confirm icon animation starts immediately.
    3) Start recording, confirm red `record.circle.fill` + filled red pill appears and icon animation runs continuously while recording remains active.
    4) Stop recording, confirm red pill is removed during pendingFinalize/finalizing and stays absent in idle; confirm finalizing animation plays and then settles in idle.
    5) Repeat on bright, dark, and high-detail wallpapers in both light and dark appearances; confirm indicator remains unambiguous.
  </how-to-verify>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemVisualStateTests -only-testing:EchoRecorderTests/StatusItemControllerIconTests</automated>
  </verify>
  <acceptance_criteria>
    - Automated verification command exits 0 before manual sign-off
    - Human confirms "visible" result for all six matrix cells (3 wallpapers x 2 appearances)
    - Human confirms red pill never appears outside `.recording`
    - Human confirms preparing/finalizing are animated and recording animation is continuous until stop
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or provide failing matrix cells and screenshots.</resume-signal>
  <done>Manual IND-02 perceptual verification is complete and phase can proceed to verify-work.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemVisualStateTests -only-testing:EchoRecorderTests/StatusItemControllerIconTests`, then complete the human visibility matrix checkpoint.
</verification>

<success_criteria>
- ICON-04: icon updates are asserted at <=100ms from state publication.
- IND-02: appearance-aware red pill passes manual visibility matrix against bright/dark/high-detail wallpapers.
- Accessibility label tracks every recorder state transition.
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-bar-iconography-recording-indicator/05-02-SUMMARY.md`
</output>
