---
phase: 05-menu-bar-iconography-recording-indicator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EchoRecorder/UI/MenuBar/StatusItemVisualState.swift
  - EchoRecorderTests/UI/StatusItemVisualStateTests.swift
autonomous: true
requirements: [ICON-01, ICON-02, ICON-03, IND-01, IND-03]
must_haves:
  truths:
    - "Idle and preparing menu bar states render recordingtape icon with no red pill"
    - "Recording state renders red record.circle.fill icon with red pill visible"
    - "Finalizing state renders externaldrive.badge.checkmark and red pill is hidden"
    - "Leaving recording immediately removes red pill in pendingFinalize/finalizing/idle"
  artifacts:
    - path: "EchoRecorder/UI/MenuBar/StatusItemVisualState.swift"
      provides: "Deterministic RecorderState -> visual mapping contract"
      exports: ["StatusItemVisualState", "statusItemVisualState(for:appName:)"]
    - path: "EchoRecorderTests/UI/StatusItemVisualStateTests.swift"
      provides: "Unit coverage for icon and indicator mapping across all recorder states"
      contains: "testAllRecorderStatesMapToExpectedVisualState"
  key_links:
    - from: "EchoRecorder/Core/Recording/RecorderState.swift"
      to: "EchoRecorder/UI/MenuBar/StatusItemVisualState.swift"
      via: "statusItemVisualState(for:appName:) switch over all enum cases"
      pattern: "switch state"
    - from: "EchoRecorderTests/UI/StatusItemVisualStateTests.swift"
      to: "EchoRecorder/UI/MenuBar/StatusItemVisualState.swift"
      via: "@testable import mapping assertions"
      pattern: "statusItemVisualState\(for:"
---

<objective>
Define and lock the recorder-state-to-icon contract before AppKit wiring so implementation cannot drift from Phase 5 decisions.

Purpose: Make ICON-01/02/03 and IND-01/03 deterministic and test-first.
Output: New visual-state mapper + exhaustive unit tests for all recorder states.
</objective>

<execution_context>
@/Users/tschaack/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/tschaack/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-menu-bar-iconography-recording-indicator/05-CONTEXT.md
@.planning/phases/05-menu-bar-iconography-recording-indicator/05-RESEARCH.md
@EchoRecorder/Core/Recording/RecorderState.swift
@EchoRecorder/UI/MenuBar/StatusItemController.swift

<interfaces>
From EchoRecorder/Core/Recording/RecorderState.swift:
```swift
enum RecorderState: Equatable {
    case idle
    case preparing
    case recording
    case finalizing
    case pendingFinalize
}
```

From EchoRecorder/UI/MenuBar/StatusItemController.swift:
```swift
@MainActor
final class StatusItemController: NSObject, StatusItemControlling {
    let configuredTitle: String
    private let recorderCoordinator: RecorderCoordinator
    private let viewModel: RecordingViewModel
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StatusItemVisualState contract and pure mapper</name>
  <read_first>
    - EchoRecorder/Core/Recording/RecorderState.swift
    - EchoRecorder/UI/MenuBar/StatusItemController.swift
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-CONTEXT.md
    - EchoRecorder/UI/MenuBar/StatusItemVisualState.swift
  </read_first>
  <files>EchoRecorder/UI/MenuBar/StatusItemVisualState.swift</files>
  <behavior>
    - Test 1: idle and preparing map to symbolName "recordingtape", symbolWeight .medium, showRecordingPill false
    - Test 2: recording maps to symbolName "record.circle.fill", uses colored symbol mode, showRecordingPill true
    - Test 3: finalizing maps to symbolName "externaldrive.badge.checkmark" and pendingFinalize keeps showRecordingPill false
  </behavior>
  <action>Create `StatusItemVisualState` with concrete fields `symbolName`, `symbolWeight`, `showRecordingPill`, `usesPaletteColor`, `accessibilityLabel`, and `isAnimated`; implement `statusItemVisualState(for:appName:)` as a full `switch` over `RecorderState` with locked values: idle/preparing=`recordingtape`, recording=`record.circle.fill`, finalizing=`externaldrive.badge.checkmark`, pendingFinalize=`recordingtape`; set `showRecordingPill=true` only for `.recording`; set accessibility labels exactly `"{appName} idle"`, `"{appName} preparing"`, `"{appName} recording"`, `"{appName} pending finalize"`, `"{appName} finalizing"`; set `isAnimated=true` for preparing/recording/finalizing and false otherwise.</action>
  <verify>
    <automated>xcodebuild build -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorder/UI/MenuBar/StatusItemVisualState.swift` exists and exports `struct StatusItemVisualState`
    - `EchoRecorder/UI/MenuBar/StatusItemVisualState.swift` contains `func statusItemVisualState(for state: RecorderState, appName: String) -> StatusItemVisualState`
    - Mapper includes explicit `case idle`, `case preparing`, `case recording`, `case pendingFinalize`, `case finalizing`
    - File contains `"recordingtape"`, `"record.circle.fill"`, and `"externaldrive.badge.checkmark"`
  </acceptance_criteria>
  <done>Mapper compiles and encodes all locked icon + indicator decisions without touching recorder transition logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create exhaustive mapper tests and assertions</name>
  <read_first>
    - EchoRecorder/UI/MenuBar/StatusItemVisualState.swift
    - EchoRecorderTests/UI/RecordingViewModelTests.swift
    - .planning/phases/05-menu-bar-iconography-recording-indicator/05-RESEARCH.md
    - EchoRecorderTests/UI/StatusItemVisualStateTests.swift
  </read_first>
  <files>EchoRecorderTests/UI/StatusItemVisualStateTests.swift</files>
  <behavior>
    - Test 1: idle/preparing use recordingtape and no recording pill
    - Test 2: recording uses red record.circle.fill and recording pill is visible
    - Test 3: leaving recording removes red pill in pendingFinalize/finalizing/idle
  </behavior>
  <action>Create `StatusItemVisualStateTests` under `EchoRecorderTests/UI` with at least these tests: `testIdleAndPreparingUseRecordingTapeSymbol`, `testRecordingUsesRedRecordCircleFill`, `testAllRecorderStatesMapToExpectedVisualState`, `testRecordingShowsRedPillOnly`, `testLeavingRecordingRemovesRedPill`; assert concrete symbol names, `showRecordingPill`, and accessibility labels for each state string.</action>
  <verify>
    <automated>xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemVisualStateTests</automated>
  </verify>
  <acceptance_criteria>
    - `EchoRecorderTests/UI/StatusItemVisualStateTests.swift` exists and declares `final class StatusItemVisualStateTests: XCTestCase`
    - File contains all five required test method names from the action
    - Running the `<automated>` command exits 0
    - Tests assert mapping for every `RecorderState` case
  </acceptance_criteria>
  <done>All mapping requirements are executable and enforced by fast unit tests.</done>
</task>

</tasks>

<verification>
Run `xcodebuild test -project EchoRecorder.xcodeproj -scheme EchoRecorder -destination 'platform=macOS' -only-testing:EchoRecorderTests/StatusItemVisualStateTests` and confirm green.
</verification>

<success_criteria>
- ICON-01/02/03 mappings are codified in a single pure mapper with no ambiguous branches.
- IND-01 and IND-03 red-pill visibility logic is encoded as `recording`-only and covered by tests.
- Unit tests fail if any symbol name, state mapping, or accessibility string regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-bar-iconography-recording-indicator/05-01-SUMMARY.md`
</output>
